version: "2"
servers: [ ]
sql:
    - engine: "postgresql"
      queries: "db/queries/"
      schema: "db/migrations/"
      gen:
          go:
              package: "db"
              out: "db/gen/queries.go"
              sql_package: "pgx/v5"

plugins: [ ]
rules: [ ]
options: { }
